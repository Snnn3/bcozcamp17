{
  "name": "bcozcamp17",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "dev:web": "npm run dev --workspace @bcoz/web",
    "dev:staff": "npm run dev --workspace @bcoz/staff",
    "dev:server": "npm run dev --workspace @bcoz/server",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "typecheck": "npm run db:generate && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:packages": "npm run build --workspace @bcoz/config && npm run build --workspace @bcoz/validation && npm run build --workspace @bcoz/api && npm run build --workspace @bcoz/auth && npm run build --workspace @bcoz/db && npm run build --workspace @bcoz/storage && npm run build --workspace @bcoz/ui",
    "build:apps": "npm run build --workspace @bcoz/web && npm run build --workspace @bcoz/staff && npm run build --workspace @bcoz/server",
    "build": "npm run db:generate && npm run build:packages && npm run build:apps",
    "check": "npm run format:check && npm run lint && npm run typecheck && npm test && npm run build",
    "db:generate": "npm run prisma:generate --workspace @bcoz/db",
    "db:migrate": "npm run prisma:migrate --workspace @bcoz/db",
    "db:seed": "npm run prisma:seed --workspace @bcoz/db",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/jest-dom": "^6.0.0",
    "@testing-library/react": "^16.0.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "oxfmt": "0.68.0",
    "oxlint": "1.83.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vite": "^8.3.0",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^5.0.1"
  }
}
