# Authentication package

This package owns server-side identity and permission types. The confirmed
authentication flow is Google OpenID Connect with a server-managed local
session; local passwords and arbitrary browser-supplied Google tokens are not
part of the project contract.
