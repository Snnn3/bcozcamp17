# API contract package

Shared request, response, and stable error schemas live here. The API server is
the authoritative boundary for validation, authorization, lifecycle transitions,
and transaction outcomes.
