# Private storage package

This package is server-only. It creates narrowly scoped, short-lived signed
URLs for private S3-compatible storage. Browser applications must never receive
permanent storage credentials or raw storage keys.
